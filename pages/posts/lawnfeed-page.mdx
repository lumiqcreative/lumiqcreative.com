import Post from '../../components/post'
import MarkdownContainer from '../../components/markdown-container'
import PostImage from '../../components/post-image'

export const meta = {
  title: 'Lawnfeed Page',
  subtitle:
    'Download page for the add-in that integrates Google Feed into Lawnchair.',
  date: '2020-07-30',
  image: '/lawnfeed-page.png'
}

export default ({ children }) => <Post meta={meta}>{children}</Post>

<MarkdownContainer>
  <PostImage src='/lawnfeed-page.png' alt='Lawnfeed Page' cover />
</MarkdownContainer>

<MarkdownContainer narrow>

I’ve designed and developed the download page for Lawnfeed –
the add-in that integrates Google Feed into Lawnchair. [Visit it here](https://lawnchair.app/lawnfeed).

</MarkdownContainer>
